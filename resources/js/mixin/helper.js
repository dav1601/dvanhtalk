export default {
    methods: {
        getAssetsPatch(patch = "") {
            console.log(window.location);
        },
        isEmpty(obj) {
            return Object.keys(obj).length === 0;
        },
      
    },
};
